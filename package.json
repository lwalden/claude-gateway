{
  "name": "claude-gateway",
  "version": "0.2.0",
  "description": "Local HTTP gateway to Claude — subscription-only via the Claude CLI (OAuth); no API key",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest --verbose"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
