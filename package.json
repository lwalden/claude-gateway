{
  "name": "claude-gateway",
  "version": "0.1.0",
  "description": "Local HTTP gateway to Claude — CLI-first (subscription), Anthropic API fallback",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
